{
  "name": "issue-1711-babel-version",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "mocha build",
    "pretest": "npm run build",
    "build": "babel src -d ./build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "mocha": "^5.0.1",
    "sinon": "^4.4.2"
  },
  "dependencies": {
    "chai": "^4.1.2"
  }
}
